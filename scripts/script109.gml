///handle_removeFromLayer(obj_layer layer, par_object obj)

/*
    This scripts removes all references to this object in 
    this layer.  Called when moving a robot from one layer
    to another.  Init room script will cause a problem if 
    it's not Rob.  TODO for Roberta, or another robot.  
*/
